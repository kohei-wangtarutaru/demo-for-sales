export default function Home() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1 style={{ fontSize: 28, marginBottom: 8 }}>営業デモ：表示テスト</h1>
      <p style={{ color: "#555", lineHeight: 1.7 }}>
        ここが表示されれば、Vercel上でNext.jsが正常に動いています。<br />
        次のステップで「店舗入力 → 9グリッド → 投稿モーダル」へ進みます。
      </p>
    </div>
  );
}
